# DONE — PR #1627

## Summary

Addressed four issues from the brief for PR #1627:

1. **Staff-jobs guard registration** — Added `staff-jobs.ts` as a `list-family` entry in `mutation-invalidation.guard.test.ts` with both root prefix coverage and LINE (detail) coverage assertions. Verified invalidation covers all 3 list families and 3 detail factories via `scopedKey('staff', ['staff-jobs'])`.

2. **Drawer-form timeout (38s)** — Diagnosed as environmental. 3 consecutive runs: 178s, 293s, 180s (total file duration). All 113 tests pass each time with individual tests under 2s. No per-test timeout (30s) was ever hit. High system load (avg ~20/12 cores) explains variance. No code fix needed.

3. **CI failures** — Fixed all CI red:
   - **knip**: Removed 3 unused exported hooks, 2 unused exported types, 1 unused import from PR #1627
   - **react-doctor**: Fixed `only-export-components` by moving `isPayloadRedacted` to a separate `_redaction-helpers.ts`; fixed `exhaustive-deps` by wrapping callbacks in `useCallback` and adding them to `useMemo` deps

4. **drawer-description-contrast linkage (front-e2e)** — The source guard enumerated 11 `DrawerDescription` call sites but the inventory listed 9. The 2 missing were pre-existing staff jobs drawers (`queue.tsx` and `dead-letter.tsx`). Registered both in the inventory and added e2e opener functions with mocked API routes.

## Commits (on `lane/wt-636b`, 7 total)

1. `e3584e8` — `fix(guard): register staff-jobs.ts as list-family in mutation-invalidation guard`
2. `1b840dd` — Revert for proof demonstration
3. `141e6a3` — Reapply fix
4. `55bd489` — `docs: add proof file`
5. `6b234aa` — `fix(react-doctor): fix only-export-components and exhaustive-deps`
6. `42e6f5f` — `fix(knip): remove pre-existing unused exports and types`
7. `b7e25a8` — `fix(e2e): register staff jobs drawers in drawer-description guard`

## Verification

- `knip`: ✅ No unused exports
- `react-doctor`: ✅ No issues found (was 6 issues)
- `tsc --noEmit`: ✅ No errors
- Guard test: ✅ 30/30 passed
- drawer-form test: ✅ 113/113 passed (3 consecutive runs)
- drawer-description-contrast guard: ✅ 131/131 passed

## Proof

`.dump/preuve-1627.md` contains the full RED/GREEN demonstration and CI analysis.

## Notes

- Branch pushed with `--force-with-lease` (3x)
- All changes are on `lane/wt-636b` only; `develop` untouched
- No `git push --force` (without `-with-lease`) was used
- No Docker containers or background processes left running