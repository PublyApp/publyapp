# DONE — Issue #1623 (reprise)

Arbre : `/home/radan/Projects/PublyApp/publyapp/.worktrees/wt-deps-clean`, branche
`lane/deps-cleanup`. Tout est pousse ; la PR est ouverte.

## Étapes executees

1. **Preuves rapides (build/typecheck)** — `front` typecheck GREEN. L'API build a
   revele une regression reelle du commit de retrait `ec7089c99` :
   `CS1061: 'LoggerEnrichmentConfiguration' does not contain a definition for
   'WithMachineName'`. La preuve d'usage etait fausse : `WithMachineName()` vient
   de `Serilog.Enrichers.Environment`, pas de `Serilog.Enrichers.Thread`
   (qui n'expose que `WithThreadId`/`WithThreadName`). => `Serilog.Enrichers.Environment`
   a ete **restaure** (commit `f59772210`). C'est le garde-fou qui a prouve que la
   reference etait utilisee.

2. **Controle de securite (preuve manquante)** — `pnpm audit --prod
   --audit-level=high` et `pnpm audit --audit-level=moderate` : tous deux
   « No known vulnerabilities found ». Copies completes dans
   `.dump/preuve-audit.md`. Aucune alerte `nanoid`, donc les plafonds pouvaient
   etre retires.

3. **preuve-absence-usage.md complete** — correction : `nanoid` n'est pas
   orphelin ; il reste comme dependance transitive de `postcss@8.5.25`
   (`nanoid@3.3.18`, version corrigee). `pnpm why nanoid` renvoie vide uniquement
   parce que le paquet n'est plus hoiste sans les overrides.

4. **Documentation corrigee** — `docs/guides/dependency-health.md` (paragraphe
   `nanoid`) mis a jour : les deux caps et la declaration directe sont retires,
   le seul consommateur restant est transitif et deja sur version saine. Le test
   `dependency-health-pin-location.test.ts` ne couvre que le paragraphe
   `@microsoft/kiota-http-fetchlibrary` (#880) — il passe toujours (2/2) et n'a
   pas ete desactive.

5. **PR ouverte** — `gh pr create --base develop --head lane/deps-cleanup`
   => **https://github.com/PublyApp/publyapp/pull/1625**. Corps contient
   `Closes #1623` (verifie via `gh api`, non via `gh pr edit`). Mentionne que
   **#1581** devient sans objet (non fermee).

## Commits pousses

- `ec7089c99` chore(deps): remove three dead packages (#1623)  [preexistant]
- `f59772210` fix(deps): restore Serilog.Enrichers.Environment (#1623)
- `e1afb240c` docs: reflect nanoid overrides removal in dependency-health (#1623)

## Non verifie (signale dans le PR)

- Timeout 2 min de la generation OpenAPI (`dotnet-getdocument.dll`) lors du
  `dotnet build` : environnemental (l'outil reussit en ~5s seul, compilation
  propre, 0 `error CS`). Non traite ici, conformement a l'instruction de ne pas
  alourdir la file verrouillee.
- Pas de `just ci` / e2e (file saturatee, instruction du brief).

## Regles respectees

- Commite et pousse apres chaque etape.
- Aucun test ni regle desactive pour faire passer une porte.
- Aucun sous-agent lance.
- Arret apres le push + ce compte rendu ; pas de `gh pr checks` en boucle,
  pas d'attente, pas de `sleep`.
