# Plan d'implémentation — #317 : cœur C# partagé + hôtes dédiés api / worker / scripts

> **Pour les agents exécutants :** plan à tâches réduites, chaque tâche = un commit poussé, relisable et réversible seule. Ne pas fusionner plusieurs tâches en un seul bloc : une migration d'architecture livrée d'un bloc est irrelisable et irréversible. Aucun sous-agent (`opencode`/`claude`/`codex` bloqués, exit 86 — captain 2026-08-23 15:30) ; le travail est fait séquentiellement dans cette lane.

**Livrable visé :** `apps/api` devient l'hôte web seul, un nouveau projet `packages/shared-cs` porte le cœur (domaine, accès données, contrats), `apps/worker` devient l'hôte de jobs, `packages/scripts-cs` devient l'hôte CLI opérationnel — et `APP_ROLE` cesse d'être le mécanisme de découpage.

**Politique de citation :** les numéros de ligne ci-dessous valent pour le tip `origin/develop` `ebdf2b6b6517e84b8f052b26cf93568e4c5dc949` (`ebdf2b6b6`) uniquement, chacun prouvé par `git show … | sed -n` / `git grep -n` avec verdict PASS/FAIL par ligne dans `.dump/citations-r2.md` (registre RÉGÉNÉRÉ round 2, exécuté réellement contre `ebdf2b6b6`, **36 citations C1-C36, 0 FAIL** — 31 citations d'état des lieux C1-C31 + 5 citations d'inventaire R2 C32-C36 prouvant les faits nouveaux de ce round : réflexion sur assembly, `[Service]` (43), `IEntityTypeConfiguration<T>` (27), précédents Roslyn). Le registre r1 (`.dump/citations-r1.md`) est obsolète : il nommait le tip `6d716e3e6` et portait 4 lignes FAIL provenant de numéros de ligne périmés ; ne pas s'y fier. Les symboles créés par les tâches sont marqués comme tels. Re-vérifier chaque numéro contre le tip COURANT avant de s'y fier : `git show origin/develop:<chemin> | sed -n '<ligne>p'`.

---

## 0. État des lieux vérifié (tip `ebdf2b6b6`)

- `apps/api` = **un seul `.csproj`** exécutable : `apps/api/PublyApp.Api.csproj` (Sdk `Microsoft.NET.Sdk.Web`), plus le projet de tests `apps/api/Tests/PublyApp.Api.Tests.csproj`. Aucun autre `.csprod` sous `apps/` (citation C27 du registre).
- La séparation se fait **à l'exécution** par `APP_ROLE` : `apps/api/Program.cs:73` (« APP_ROLE decides composition (design §3.2) », C1), branche worker ligne 100 (`role is AppRole.Worker`, C4), compositions publiques `CreateWorkerHostBuilder` (:169) et `CreateWebHostBuilder(string[], AppRole)` (:189, C5).
- Rôles : `public enum AppRole { Api, Worker, All }` (`apps/api/Lib/AppRole.cs:18`, C9). Parse fail-fast : `GetOptionalAppRole` (`apps/api/Lib/AppEnvironment.cs:856`, C8).
- Trois CLI embarquées dans le binaire web : `BulkSeedCli.TryRun` (`Program.cs:63`, C2 ; classe en `apps/api/Lib/Seeding/BulkSeedCli.cs:11`, C26), `WorkerHealthCli.TryRun` (`Program.cs:69`, C3 ; `TryRun` en `apps/api/Infrastructure/Jobs/WorkerHealthCli.cs:22`), `HostedServiceManifestCli.TryRun` (`apps/api/Lib/Diagnostics/HostedServiceManifestCli.cs:23`) et `SeederGateProbeCli`.
- Contrats de santé : `/health/live` :326, `/health/ready` :331, `/health` :335 mappés dans `ConfigureHttpPipeline` (`apps/api/Program.cs`, C6) ; disponibilité migration = `DatabaseMigrationHealthCheck` (`apps/api/Infrastructure/Health/DatabaseMigrationHealthCheck.cs:9`, C23) sur `IDatabaseMigrationReadiness` (`DatabaseMigrationReadiness.cs:7`, C23) ; vivacité worker = battement de cœur fichier `WorkerHeartbeat` (`apps/api/Infrastructure/Jobs/WorkerHeartbeat.cs:12`, C21) écrit par `WorkerHeartbeatService` (`WorkerHeartbeatService.cs:17`, C21), enregistré par `AddWorkerServices` → `AddHostedService<WorkerHeartbeatService>` (`apps/api/Infrastructure/Jobs/JobsServiceRegistration.cs:89`, C22), sondé hors HTTP par `--worker-health` (healthcheck `dokploy.yml:178`, C12).
- Sémantique de migration #885 : **qui applique** = le conteneur one-shot `publyapp-migrate` (image `migrate`, bundle EF `dotnet-ef migrations bundle`, `apps/api/Dockerfile:64`, C14 ; `APP_ROLE=api` sur ce service `dokploy.yml:205`, C12). **Qui attend** = l'API (readiness `/health/ready` portée par `DatabaseMigrationHealthCheck` : Traefik ne route pas tant que ce n'est pas vert, budget `start_period: 300s` `dokploy.yml:110`, C12) et le worker (`WorkerMigrationStartupGate.StartAsync`, classe :24 / `StartAsync` :47, `apps/api/Infrastructure/Jobs/WorkerMigrationStartupGate.cs`, C20, timeout 5 min, heartbeat maintenu frais pendant l'attente).
- Génération OpenAPI : `dotnet build` **exécute l'app** pour émettre `openapi.json` ; `APP_ROLE=api` y est requis car l'environnement hôte résout alors en Production. Pins : `justfile:492` (`generate-client $APP_ROLE="api"`), `justfile:111` (`build-api`), `justfile:211` (`db-migrate`), C16 ; `quality-gate.yml` force `APP_ROLE="api"` (ex. :179, :203, :210, :223, C29) ; le Dockerfile contient exactement **deux** blocs `ASPNETCORE_ENVIRONMENT=Production` (C15) portant tout le jeu de variables requis, gardé par `AppEnvironmentBuildEnvCompleteness.Spec` qui lit la vraie source `AppEnvironment.cs` (:157, C10) et exige chaque variable **dans chaque** bloc (:212/:222, C11).
- Déploiement : trois images par release — `api` (target `runtime`), `migrate` (target `migrate`), `front` (`.github/workflows/deploy-images.yml:60-83`, C13). `dokploy.yml` fait tourner `publyapp-api` (`APP_ROLE=api`, :18) et `publyapp-worker` (`APP_ROLE=worker`, :129) depuis **la même image**, plus le one-shot `migrate` et `front` (C12).
- Hôte scripts existant : `packages/scripts-cs/PublyApp.Scripts.csproj`, `Program.cs` dispatchant `generate-translation-keys` (C31) — référencé par la solution (`PublyApp.slnx:13`, C18) et déjà branché en codegen de clés i18n par `PublyApp.Api.csproj` (target `GenerateTranslationKeys`).
- Tests : les `*.Spec.cs` co-localisés sous `apps/api/**` sont compilés par globs dans `Tests/PublyApp.Api.Tests.csproj:33-38` (C19) ; `AppRoleComposition.Spec` (`apps/api/Lib/Architecture/AppRoleComposition.Spec.cs:36`) épingle le graphe par rôle via `HostedServiceManifestCli`.
- **#255 n'est PAS livré** : zéro occurrence `Aspire|AppHost` dans l'arbre suivi (C28). La preuve de faisabilité existe (gate `spike/255-apphost-gate`, commentaire #255 du 2026-07-29 : même projet référencé deux fois, `APP_ROLE=api` et `APP_ROLE=worker` injectés, socket uniquement sur l'instance api) mais aucun AppHost dans le dépôt.
- Correction dotenv #1027 **déjà fusionnée** sur develop (`e17d262f4 fix(api): stop the development env file from overriding explicit environment variables (#1019) (#1027)`) : une variable processus explicite gagne sur `.env.development`. C'est elle qui rend injectable par un orchestrateur — et donc compatible AppHost — toute variable des deux futurs hôtes.

## 1. Ordonnancement : présupposés sur #255, et variante sans #255

L'arbitrage propriétaire du 2026-08-26 (#317 commentaire 11:35, `.dump/reouvertures-2026-08-26/dec-317.md`) fixe l'ordre : **#255 d'abord, ce chantier ensuite**. Ce plan respecte cet ordre et rend explicite sa dépendance :

**Ce que ce plan présuppose de #255 (et rien de plus) :**
1. Un projet AppHost C# existe quelque part sous `apps/` (nom et chemin décidés par #255 ; ce plan ne les présume pas).
2. L'AppHost sait lancer **un même projet exécutable deux fois avec des environnements différents** (prouvé par la gate #255 du 2026-07-29) et déclarer les ports côté AppHost (launch profiles désactivés).
3. L'orchestration locale couvre PostgreSQL + API + worker + front avec un seul démarrage, traces/métriques visibles.

**Ce que ce plan fait SI #255 n'est pas encore livré au moment d'exécuter une tâche donnée :**
- Les phases A et B n'en dépendent **pas** : elles produisent des projets exécutables ordinaires. Le pont local est alors un `just dev-worker` (même motif que `dev-api` : `cd apps/worker && dotnet watch run --no-restore -property:OpenApiGenerateDocuments=false`) — un terminal de plus, temporairement, précisément la situation que l'arbitrage veut éviter ; c'est pourquoi les tâches marquées `[post-#255]` sont à faire dès que l'AppHost existe, sans attendre la suite.
- Les étapes `[post-#255]` (§6, T16) ajoutent les hôtes à l'AppHost : worker lancé depuis son propre projet (fini le truc des deux références du même projet avec `APP_ROLE` différent), scripts jamais orchestrés (CLI one-shot, pas une ressource longue).
- Si #255 atterrit PENDANT ce chantier, T16 se fait immédiatement après T13, avant T14/T15.

## 2. Contraintes durement acquises — chacune nommée, chacune suivie

| Contrainte | État actuel (prouvé §0) | Ce que ce plan exige |
|---|---|---|
| **Sémantique de migration #885** | `migrate` applique (bundle EF, Dockerfile:64/C14) ; API attend via `/health/ready`+`DatabaseMigrationHealthCheck` ; worker attend via `WorkerMigrationStartupGate` (:24/:47, C20) | Après découpage : `migrate` reste le SEUL applicateur. L'hôte web garde son readiness migration. L'hôte worker garde sa porte de démarrage ET son heartbeat pendant l'attente. Aucun nouvel hôte n'acquiert le droit d'appliquer. `just db-migrate`/`db-add` continuent de fonctionner sans `APP_ROLE` (T14) |
| **Contrats de santé** | `/health/live`, `/health/ready`, `/health` (C6) ; readiness migration (C23) ; heartbeat fichier worker + `--worker-health` (`dokploy.yml:178`, C12) | Endpoints et sémantiques inchangés sur l'hôte web. L'hôte worker conserve le heartbeat fichier et la sonde `--worker-health` (son healthcheck Compose n'a pas d'autre surface). Tout remplacement interdit : un hôte qui perd son contrat de santé casse le déploiement |
| **`dokploy.yml`** | Contrat écrit à la main, relu, pilotant la production en ligne (C12) | Ce plan **ne l'édite pas**. Il liste précisément les changements nécessaires au basculement (§7) à n'appliquer qu'avec l'arbitrage propriétaire, dans une PR dédiée, après que les nouvelles images existent |
| **Génération OpenAPI** | `dotnet build` exécute l'app ; `APP_ROLE=api` requis (C16/C29, Dockerfile C15, garde C11) | Quand `APP_ROLE` meurt (T14), le chemin doc-gen est remplacé par : l'hôte web compose TOUJOURS la surface web (plus de branchement), donc `build-api`/`generate-client` n'ont plus besoin de rôle ; le garde d'exhaustivité est ré-enseigné (`APP_ROLE` sort du jeu gardé) ; les pins justfile/quality-gate/.env.example/compose-test/Dockerfile sont retirés en cohérence |

## 3. Le vrai risque : le cœur fourre-tout — frontière ET garde

### 3.1 Ce qui a le DROIT d'entrer dans `packages/shared-cs` (liste initiale fermée)

Nouveau projet : `packages/shared-cs/PublyApp.Core.csproj` (Sdk `Microsoft.NET.Sdk`, `net10.0`, racine de namespace `PublyApp.Core`). Consommateurs visés : `apps/api`, `apps/worker`, `packages/scripts-cs`.

Contenu autorisé (migration depuis `apps/api`, chemins exacts) :
- `Data/` complet : `BaseAttributes.cs`, `IEntity.cs`, `IEntitySeeder.cs`, `DbSetExtensions.cs`, `RequiredNavigation.cs`, `Data/DbContext/` (dont `AppDbContext` :27, C25), `Data/Seeding/` ;
- `Migrations/` en entier (60 fichiers, `git mv` pur) — l'assembly des migrations suit l'assembly du `DbContext` ;
- `Modules/<Domaine>/Entities/` + enums/value objects de tous les domaines (25 modules). **Attention réflexion :** `OnModelCreating` fait `modelBuilder.ApplyConfigurationsFromAssembly(typeof(AppDbContext).Assembly)` (`apps/api/Data/DbContext/AppDbContext.cs:272`) et les 27 classes `IEntityTypeConfiguration<T>` sont co-localisées DANS `Modules/*/Entities/` (comptées par `git grep -rn 'IEntityTypeConfiguration<' origin/develop -- 'apps/api/Modules/**/*.cs' | wc -l` → **27**, + 1 `DataProtectionKeyConfiguration.cs` dans `Data/DbContext/` = 28 total ; le plan r1 disait à tort 26) — chaque `git mv` d'un dossier `Entities/` emporte donc ses configurations EF, et AUCUNE ne doit rester hors de Core ;
- `Modules/<Domaine>/Services/` ne dépendant que de `DbContext` + infrastructure (règle house : « services implement, handlers orchestrate ») — c'est le lot consommé par les deux hôtes ;
- Contrats jobs : `Infrastructure/Jobs/JobJson.cs`, définitions `JobDefinition<TPayload>`, `IJobEnqueuer` + `EnqueueOptions`, registre — les producteurs tournent dans CHAQUE hôte aujourd'hui (`AddJobProducerServices` appelé dans les deux compositions, `Program.cs:177`/`:198`) ;
- Infrastructure trans-hôtes effectivement consommée par ≥ 2 hôtes : `Lib/Extensions/LoggerConfigExtensions.cs` (logger Serilog + sanitiser), `Modules/SocialAccounts/Infrastructure/SocialAccountsMasterKeyWitness.cs` + `PostgresKeyRingCanaryStore` (appelés par LES DEUX boots, `Program.cs:107` et `:137`), envoi e-mail (interface + impl Resend) ;
- Primitives : `PatchField.cs`, `CursorPaginatedQuery.cs`, `CursorPaginatedResult.cs`, `OffsetPaginatedQuery.cs`, `OffsetPaginatedResult.cs`, `CursorSortFieldHandler.cs`, `SortOrder.cs`, `AppPermissions.cs`, `EnvironmentNames.cs`, `SupportedLangage.cs` ;
- `AppEnvironment.cs` + ses `*.Spec.cs` (aucune dépendance ASP.NET : FluentValidation + Npgsql seulement).

**Contrainte transversale réflexion (s'applique à T3/T4/T5) :** la découverte des seeders est aussi limitée à l'assembly du DbContext — `SeederTypeCache = new(DiscoverSeedersInternal, …)` (`AppDbContext.cs:28`) balaie `typeof(AppDbContext).Assembly` à la recherche de tout `IEntitySeeder` (`DiscoverSeedersInternal`, `AppDbContext.cs:252`). Les 7 implémentations (`Modules/{Jobs,Permissions,Profiles,Tenants,Users}/Seeders/`, soit Jobs×1 + Permissions×1 + Profiles×1 + Tenants×1 + Users×3) doivent donc être dans l'assembly Core au plus tard dans le commit qui y déplace `AppDbContext`. Un seeder oublié ne casse PAS le build : il disparaît silencieusement du peuplement. Vérification obligatoire post-tranche : un spec énumère `AppDbContext`-assembly vs types attendus (comparer au décompte git avant déménagement).

### 3.2 Ce qui n'a PAS le droit d'entrer dans `packages/shared-cs`

- **Tout ce qui importe ASP.NET Core** : endpoints, groupes/filtres de route, middlewares (security headers, CORS, rate limiting, session/tenant/staff auth), `TypedProblems.*` / `App*HttpResult` / `ProblemResults/`, `ApiResponse.cs`, OpenAPI/Scalar, fichiers statiques `/files`, `Routes/`, `RequestAuthContext.cs`, règles de validation de corps (`Validation/`, `JsonElementRules`) ;
- Les types filaires de handlers (`Body`/`Query`/`Result`/`Response`/`Item`) — frontière HTTP = `apps/api` ;
- La composition web (`AddWebServices`, pipeline `ConfigureHttpPipeline`) et la composition worker (`AddWorkerServices`, processeurs Quartz, `WorkerHeartbeatService`, `WorkerMigrationStartupGate`, `WorkerHealthCli`) — chaque hôte possède sa propre composition ;
- `Lib/Testing/`, fakes et fixtures — restent dans le projet de tests ;
- Tout code « utilisé ailleurs une fois » : la règle d'entrée est **consommé par ≥ 2 hôtes, OU domaine/accès données**. Sinon ça reste dans l'hôte qui l'utilise.

### 3.3 Le garde qui empêche la dérive (à livrer AVEC la première tranche, pas après)

Nouveau spec d'architecture `packages/shared-cs/Lib/CorePurity.Spec.cs` (compilé dans le runner existant, cf. T2), deux axes :
1. **Csproj** : `PublyApp.Core.csproj` ne doit contenir ni `<FrameworkReference Include="Microsoft.AspNetCore.App" />`, ni `Sdk="Microsoft.NET.Sdk.Web"`, ni PackageReference dont le nom commence par `Microsoft.AspNetCore.` — parsé depuis le vrai fichier (motif du garde C10/C11 : lire la source réelle, pas une copie).
2. **Source** : scan Roslyn (précédents réels : `JobEnqueueBoundary.Spec.cs` sous `apps/api/Infrastructure/Jobs/` — scan lexé par `Microsoft.CodeAnalysis.CSharp`, C35 ; `ApiFactoryHostedServiceGuard.Spec.cs` sous `apps/api/Lib/Architecture/` — résout les services réels du conteneur et s'interdit un type, C35 ; le chemin `Lib/Architecture/JobEnqueueBoundary.Spec.cs` cité au r1 était erroné) de `packages/shared-cs/**/*.cs` hors `*.Spec.cs` : interdits `using Microsoft.AspNetCore.*;`, `IActionResult`/`Results.`/`EndpointFilter*`, `WebApplication`/`IEndpointRouteBuilder`. Le spec doit prouver qu'il détecte son défaut par une **mutation réaliste**, pas seulement « supprimer le garde → rouge » (ce qui ne prouve que qu'il se lit lui-même) :
   - *Preuve rouge exigée (axe csproj)* : ajouter `<FrameworkReference Include="Microsoft.AspNetCore.App" />` au `PublyApp.Core.csproj` (référence interdite par l'axe 1) et exiger que le spec tourne **ROUGE** ; retirer la référence avant le commit de la tranche. C'est la mutation qu'une lane écrirait réellement (elle « oublie » que Core n'est pas un projet web) et qu'un scanner de `using` ne verrait PAS (la référence n'apparaît que comme assembly référencé, pas comme `using` dans le source).
   - *Preuve rouge exigée (axe source)* : un fichier scratch contenant `using Microsoft.AspNetCore.Mvc;` → violations non vides (ROUGE).
   - **Angle mort assumé (à documenter dans le spec, ne pas laisser croire couvert)** : l'axe **source** ne voit PAS une référence interdite qui n'apparaît que via une `FrameworkReference` ou `PackageReference` transitive résolue à la compilation (pas de `using` dans le `.cs`). C'est exactement pourquoi la **preuve rouge csproj** ci-dessus est REQUISE : seul l'axe csproj attrape ce cas. Le garde protège donc la limite *source* par l'axe 2 et la limite *référence projet* par l'axe 1 ; une dérive par référence transitive non nommée explicitement resterait hors de portée et doit être couverte par revue humaine, pas par le spec.

Documentation jumelle : section « Frontière shared-cs » dans `AGENTS.md` (tableau autorisé/interdit + règle d'entrée ≥ 2 hôtes) — une frontière sans garde dérive en trois mois ; une garde sans règle écrite se contourne en trois revues.

### 3.4 Inventaire EXHAUSTIF des réflexions sur assembly de l'API (CRITIQUE 2 R2 — aucune omise)

Toute reflection-over-assembly liée à l'assembly d'un hôte casse silencieusement après la scission : `packages/shared-cs` (Core) et `apps/worker` deviennent des assemblys séparés de `apps/api`, et `typeof(Program).Assembly` / `typeof(AppDbContext).Assembly` ne contiennent plus ce qui en a été extrait. Voici **chaque** site trouvable par `git grep -n 'typeof(' … | grep -i Assembly` dans `apps/api/**/*.cs` : **13 sites de réflexion distincts** (14 appels réels `typeof(X).Assembly` + 1 commentaire explicatif en `EntityConfigurationConvention.Spec.cs:71` = 15 lignes grep, citation C32). **Note (scan élargi, citation C36) :** un grep plus large sur tous les motifs d'énumération d'assembly (`AppDomain.CurrentDomain.GetAssemblies`, `Assembly.Get(Executing|Entry)Assembly`, `GetCallingAssembly`, `Assembly.Load`, `GetType().Assembly`) ne révèle qu'UN site supplémentaire non capturé ci-dessus : `PublicationArchitecture.Spec.cs:111` (`AppDomain.CurrentDomain.GetAssemblies()`), un fixture de test Roslyn qui lit aussi le disque (`AppContext.BaseDirectory`, `SharedFrameworkAssemblies()`) et N'est PAS lié à l'assembly d'un hôte — il ne casse donc pas silencieusement après scission (contrairement à `ServiceScanner`/`ServiceAttributeRegistration`). Total des sites de réflexion sur assembly = **14** (13 via `typeof(X).Assembly` + 1 host-agnostique). Avec son devenir après scission et la tâche qui le traite :

| # | Site (chemin : ligne) | Ce qu'il fait | Devenir après scission | Tâche |
|---|---|---|---|---|
| 1 | `Data/DbContext/AppDbContext.cs:254` | `var assembly = typeof(AppDbContext).Assembly;` (cache seeders) | **Inchangé** : AppDbContext vit en Core, donc `typeof(AppDbContext).Assembly` est déjà Core. Reste correct. | T3 |
| 2 | `Data/DbContext/AppDbContext.cs:272` | `ApplyConfigurationsFromAssembly(typeof(AppDbContext).Assembly)` | **Inchangé** : même assembly que les configs (déplacées avec les entités). Reste correct. | T4 |
| 3 | `Lib/AppEnvironmentDotEnvPrecedence.Spec.cs:652` | `typeof(Program).Assembly.Location` (spec test, répertoire d'exécution) | **Inchangé** : spec de précédence dotenv, lit l'assembly de l'hôte de test (apps/api). Pas de dépendance à Core. | T1/T2 (runner) |
| 4 | `Lib/AppEnvironmentDotEnvPrecedence.Spec.cs:685` | idem (:652) | idem | T1/T2 |
| 5 | `Lib/Architecture/AppRoleComposition.Spec.cs:317` | `typeof(Program).Assembly.Location` (résout le graphe de l'hôte réel) | **Changé** : ce spec devient `ApiComposition.Spec`/`WorkerComposition.Spec` (T14) et pointe `typeof(PublyApp.Api.Program)` / `typeof(PublyApp.Worker.Program)`. | T14 |
| 6 | `Lib/Architecture/ArchitectureDiscovery.cs:222` | `return typeof(Program).Assembly;` (découverte handlers/validateurs/routes par l'assembly api) | **Changé** : la découverte des handlers/validateurs/routes RESTE liée à l'assembly api (ces types ne bougent pas en Core). `Program` reste `PublyApp.Api.Program`. **À NE PAS basculer sur Core.** | T11 |
| 7 | `Lib/Architecture/EntityConfigurationConvention.Spec.cs:72,96` | `typeof(AppDbContext).Assembly` (scan de configs EF, miroir du site 2) | **Inchangé** : même assembly que AppDbContext (Core). Reste correct. | T4 |
| 8 | `Lib/Architecture/PublicationArchitecture.Spec.cs:185` | `typeof(object).Assembly.Location` (chemins framework BCL, pas notre assembly) | **Inchangé** : ne regarde pas notre assembly. | — |
| 9 | `Lib/Architecture/SeederGateProbeSpec.Spec.cs:59` | `typeof(PublyApp.Api.Program).Assembly.Location` | **Changé** : sonde de seeders liée à l'assembly de démarrage. Après T3 (AppDbContext en Core) la sonde doit viser `typeof(PublyApp.Core.Program)` ou balayer multi-assembly. | T3 |
| 10 | `Lib/DI/ServiceAttributeRegistration.Spec.cs:185` | `Assembly assembly = typeof(Program).Assembly;` puis `GetTypes()` filtré `PublyApp.Api.Modules.*.Services` | **BLOQUANT R2** : ce spec compte/valide les services `[Service]` de `apps/api`. Après T5 (43 classes `[Service]` déplacées en Core, citation C33), `typeof(Program).Assembly` ne les contient plus. **Corrigé** : balayage multi-assembly (`apps/api` + `packages/shared-cs`), ou marqueur `typeof(PublyApp.Core.Program)`. | T5 |
| 11 | `Lib/DI/ServiceScanner.cs:28` | `ScanAssembly(typeof(TAssemblyMarker).Assembly)` — c'est le moteur de `ServiceRegistration.ValidateServiceAttributes` | **BLOQUANT R2** : `ServiceRegistration.cs:261` appelle `ServiceScanner.ScanAssembly<Program>()` → après T5, les 43 services Core ne sont plus dans `typeof(Program).Assembly`, ils **cessent silencieusement de s'enregistrer** (pas d'erreur de compilation, échec à la 1re résolution de dépendance). **Corrigé** : `ScanAssembly<PublyApp.Core.Program>()` (ou `ScanAssembly` multi-assembly sur api + Core) + AppHost/api/worker/ scripts héritent du balayage. | T5 |
| 12 | `Lib/Testing/Diagnostics/CanaryBootLogCli.cs:79` | `typeof(Program).Assembly.Location` (CLI de diagnostic boot) | **Changé** : reste lié à l'hôte qui boote (api ou worker). Après T10/T11, la CLI est dupliquée ou partagée ; pointer le bon `Program` par hôte. | T10/T11 |
| 13 | `Modules/Profiles/Validation/ProfileStyleValidationRules.Spec.cs:10` | `typeof(ProfileStyleValidationRules).Assembly` (spec unitaire, type interne au module) | **Inchangé** : le type voyage avec son assembly ; reste correct où qu'il soit. | T4 |

**Contre-mesure obligatoire (le test qui ROUGIT si un service `[Service]` cesse de s'enregistrer) — exigence de la CRITIQUE 2 :**

Aucun des specs ci-dessus ne COMPTE les services résolus dans le conteneur réel. Il faut, livré avec T5, un spec `DiServiceRegistration.Completeness.Spec` (ou l'extension de `ServiceAttributeRegistration.Spec`) qui :

1. compose le conteneur réel de l'hôte api via la `WebApplicationFactory`/composition test existante (`ApiFixture` / `HostedServiceManifestCli` déjà en place — précédent `ApiFactoryHostedServiceGuard.Spec.cs`) ;
2. résout **après composition** (pas un modèle, pas un descripteur) `IEnumerable<DiscoveredService>` ou interroge `ServiceProvider.GetServices<T>()` pour une **liste blanche nommée** de services attendus (ex. `IUserService`, `ITenantService`, `IAuthService`, …) ;
3. **compte et nomme** les services effectivement enregistrés et échoue si le décompte attendu (43 de Core + N d'api) n'est pas atteint, ou si un service nommé manque.

Ce spec interroge le conteneur réel après composition : une perte silencieuse (services Core non balayés par le `ScanAssembly` corrigé) le fera échouer au lieu de passer au vert. C'est la contrepartie obligatoire du déplacement T5. Le plan r1 ne le contenait pas : c'est le défaut de conception bloquant de la CRITIQUE 2.

## 4. Phases et tâches (chaque tâche = un commit, poussé immédiatement)

Préambule commun à toutes les tâches : heavy commands sous `~/ai-orchestration-playbook/tools/heavy.sh`, ciblés d'abord (`dotnet test -c Test --filter "FullyQualifiedName~<Spec>"`), suite complète une fois en fin de phase, jamais > 20 min sous le lock. Jamais `git add -f`, jamais de commit sous `.dump/`. Après tout changement de contrat API : `just build-api && just generate-client && pnpm --filter front typecheck`.

### Phase A — cœur partagé (rien ne change pour les déploiements)

- **[ ] T1 — Squelette `packages/shared-cs`.** Créer `packages/shared-cs/PublyApp.Core.csproj` (Sdk `Microsoft.NET.Sdk`, `net10.0`, `<IsPackable>false</IsPackable>`), `packages/shared-cs/Directory.Build.props` sur le motif existant de `packages/scripts-cs/Directory.Build.props` (`DotNetArtifactsRoot` local puis import racine), l'ajouter à `PublyApp.slnx` (folder `/packages/`), et le référencer depuis `apps/api/PublyApp.Api.csproj` (ProjectReference simple). Tranche de code initiale minimale : déplacer `PatchField.cs`, `SortOrder.cs`, `EnvironmentNames.cs` vers `packages/shared-cs/Lib/` (namespaces `PublyApp.Core.Lib`, mise à jour mécanique des `using` consommateurs). Vérif : `just build-api` vert, suite API verte (filtrée puis complète).
- **[ ] T2 — Runner de specs traverse le nouveau paquet.** Ajouter à `apps/api/Tests/PublyApp.Api.Tests.csproj` un glob miroir : `<Compile Include="..\..\packages\shared-cs\**\*.Spec.cs" Link="Core\%(RecursiveDir)%(Filename)%(Extension)" />` (sur le motif des lignes 33-38, C19) et exclure ces mêmes sources du csprod Core (`<Compile Remove="**/*.Spec.cs" />` sur le motif de `PublyApp.Api.csproj`). Vérif : un spec scratch placé sous `packages/shared-cs` compile et tourne ; le scratch est supprimé dans le même commit.
- **[ ] T3 — Données : `Data/` + `Migrations/` déménagent.** `git mv apps/api/Data → packages/shared-cs/Data` et `git mv apps/api/Migrations → packages/shared-cs/Migrations` (namespaces → `PublyApp.Core.Data.*`, `PublyApp.Core.Migrations.*`). Ré-orienter `FindRepoFileText("apps","api",…)` des specs qui lisent ces fichiers s'ils sont visés (aujourd'hui C10 vise `AppEnvironment.cs` — traité en T8). **Découverte EF design-time (MAJEUR R2) :** `dotnet ef` / `just db-add` découvrent `AppDbContext` via l'assembly de démarrage (`apps/api`). Après ce déménagement, `AppDbContext` (+ `Migrations/`) vit dans `packages/shared-cs` — un projet non-web, non-démarrage — donc `dotnet ef migrations bundle` dans le stage `migrations` du Dockerfile (qui construit `apps/api`, C14) et `just db-add`/`just db-migrate` perdent le point d'entrée de conception. **Repli à câbler EXPLICITEMENT, tâche par tâche, dans CE même commit :** (a) Dockerfile stage `migrations` (:64) — remplacer `dotnet tool run dotnet-ef migrations bundle` par un appel pointant le projet Core : `dotnet tool run dotnet-ef migrations bundle --project packages/shared-cs/PublyApp.Core.csproj --startup-project apps/api/PublyApp.Api.csproj` (Core porte le `DbContext` + `Migrations`, api reste le projet de démarrage pour la chaîne de build) ; (b) justfile recettes `db-add`/`db-migrate` (:211 et apparentées) — ajouter `--project packages/shared-cs --startup-project apps/api` ou, mieux, une recette dérivée `db-add-core`/`db-migrate-core` pointant Core, et garder `db-migrate` fonctionnel SANS `APP_ROLE` (T14) ; (c) `just dev-api-migrated` et toute recette de migration hérite du même pointage. **Vérification obligatoire post-tranche :** reconstruire le stage `migrations` localement et confirmer la PRÉSENCE du bundle (`test -f <publish>/efbundle` ou `ls` du chemin `/publish/efbundle` du Dockerfile :64) ; exécuter `just db-add ScratchProbe && just db-remove` et `just db-migrate` verts via le repli ; `just build-api-full` vert. Le plan r1 nommait le repli (« --project packages/shared-cs ») mais ne le câblait ni dans le Dockerfile ni dans les recettes : c'est corrigé ici.
- **[ ] T4 — Entités et enums des 25 modules.** Par lots relisables (4 commits max) : `git mv` de chaque `apps/api/Modules/<Domaine>/Entities/` vers `packages/shared-cs/Modules/<Domaine>/Entities/`, namespaces `PublyApp.Core.Modules.<Domaine>.Entities`. Ordre : Tenants/Users/Auth/Invitations/Permissions (socle), puis Posts/Projects/Profiles/Settings/Uploads/SystemNotices/AuditLogs/Messaging, puis Jobs/SocialAccounts/Publishing/Calendar/Channels/Media/Analytics/Approvals/Billing/Impersonations/Account/Health. Chaque lot emporte les `IEntityTypeConfiguration<T>` co-localisés (cf. §3.1). Les dossiers `Seeders/` des domaines déjà déplacés partent dans le même commit dès lors qu'`AppDbContext` vit en Core (contrainte réflexion §3.1) ; sinon ils attendent T5. Vérif par lot : build + specs du domaine filtrés + décompte seeders découverts inchangé.
- **[ ] T5 — Services de domaine trans-hôtes.** Déplacer `Modules/<Domaine>/Services/` ne touchant ni HTTP ni filtres (les services respectent déjà « DbContext + infrastructure ») vers `packages/shared-cs/Modules/<Domaine>/Services/`. Rester sur `Publy0007` : les variantes `*ForStaff*` voyagent telles quelles (elles filtrent les suspendus, sémantique inchangée). Ne PAS déplacer un service tant que son unique consommateur est un handler HTTP : liste blanche par domaine dans le message de commit. **CRITIQUE 2 R2 — marqueur de réflexion DI à corriger DANS CE COMMIT :** les **43** classes `[Service]` déplacées (citation C33) sont aujourd'hui enregistrées par `ServiceScanner.ScanAssembly<Program>()` (`ServiceRegistration.cs:261`, moteur `ServiceScanner.cs:28`) et recomptées par `ServiceAttributeRegistration.Spec.cs:185` (`typeof(Program).Assembly`). Après le déplacement, `typeof(Program).Assembly` (api) ne contient PLUS ces services → ils **cessent silencieusement de s'enregistrer** (pas d'erreur de compilation). Corriger : (a) `ServiceScanner.ScanAssembly<PublyApp.Core.Program>()` (ou `ScanAssembly` multi-assembly sur `apps/api` + `packages/shared-cs`) ; (b) `ServiceAttributeRegistration.Spec.cs` adopte le même balayage multi-assembly ; (c) **livrer le spec `DiServiceRegistration.Completeness.Spec` (§3.4)** qui interroge le conteneur réel après composition et compte/name les services attendus — il ROUGIT si un service `[Service]` de Core manque. Vérif : `just build-api` vert ; `ServiceAttributeRegistration.Spec` + `DiServiceRegistration.Completeness.Spec` verts (ce dernier prouve que les 43 services Core sont bien résolus) ; suite API filtrée puis complète.
- **[ ] T6 — Contrats jobs.** Déplacer `Infrastructure/Jobs/JobJson.cs` + définitions `JobDefinition<TPayload>` + `IJobEnqueuer`/`EnqueueOptions` + registre vers `packages/shared-cs/Infrastructure/Jobs/`. Les consommateurs/processus (processeur, Quartz, DLQ sweepers) restent dans `apps/api` jusqu'à T12. Vérif : `JobEnqueueBoundary.Spec` et `JobsServiceRegistration`-specs verts.
- **[ ] T7 — Le garde d'abord utile : `CorePurity.Spec.cs`** (§3.3) + section `AGENTS.md`. À livrer AVANT les tranches infrastructure restantes, pour que T8 naisse sous la garde plutôt que d'y être soumis après coup.
- **[ ] T8 — `AppEnvironment` + infrastructure trans-hôtes.** Déplacer `Lib/AppEnvironment.cs` + `Lib/AppEnvironment*.Spec.cs` vers `packages/shared-cs/Lib/` ; ré-orienter `FindRepoFileText("apps","api","Lib","AppEnvironment.cs")` du garde (C10) vers `("packages","shared-cs","Lib","AppEnvironment.cs")`. Déplacer `Lib/Extensions/LoggerConfigExtensions.cs` et `Modules/SocialAccounts/Infrastructure/SocialAccountsMasterKeyWitness.cs` + `PostgresKeyRingCanaryStore` (consommés par les deux boots, `Program.cs:107/:137`). Rien d'autre : la liste §3.1 est fermée.
- **[ ] T9 — Filet complet phase A.** Suite API intégrale sous heavy.sh (une fois), `just ci-drift`, revue des namespaces résiduels (`IDE0130` doit rester vert). Aucun changement comportemental attendu : `dokploy.yml`, workflow deploy et Dockerfile intouchés à ce stade.

### Phase B — hôtes dédiés (`APP_ROLE` cesse d'être le mécanisme)

- **[ ] T10 — Naissance d'`apps/worker`.** Créer `apps/worker/PublyApp.Worker.csproj` (Sdk `Microsoft.NET.Sdk`, `FrameworkReference Microsoft.Extensions.HostBuilder` si besoin — cible : Generic Host pur, **aucun** `Microsoft.AspNetCore.App`), `apps/worker/Program.cs` : `AppEnvironment.Initialize()` → witness master-key (identique `Program.cs:107-120`) → `workerHost.Run()`. Y déménager depuis `apps/api` : `CreateWorkerHostBuilder` (composition, `Program.cs:169`), `Infrastructure/Jobs/{JobQueueProcessor,Quartz wiring,sweepers}.cs`, `WorkerHeartbeat.cs`, `WorkerHeartbeatService.cs`, `WorkerMigrationStartupGate.cs`, `WorkerHealthCli.cs`, `JobsServiceRegistration.cs` (renommé `WorkerComposition.cs` interne au projet). `apps/api` référence `PublyApp.Worker` ? **Non** : aucun lien entre les deux hôtes ; le partage passe par Core. **Indépendant de #255 :** `apps/worker` est un exécutable Generic Host ordinaire, lancé seul (recette `just dev-worker` transitoire si #255 n'est pas livré, cf. §1) ; il n'attend AUCUN artefact AppHost. Vérif : `--worker-health` répond 0/1 hors HTTP ; `WorkerMigrationStartupGate.Spec` vert.
- **[ ] T11 — `apps/api` amputé de la branche worker.** Supprimer de `apps/api/Program.cs` : la branche `role is AppRole.Worker` (:100), `CreateWorkerHostBuilder`, `WorkerHealthCli.TryRun` (:69) ; `Main` devient : probes doc-gen/probes test → witness → `CreateWebHostBuilder` (renommée composition unique) → pipeline → `Run`. `BulkSeedCli.TryRun` (:63) reste provisoirement (T13). Vérif : `AppRoleComposition.Spec` réduit au cas web, `HostedServiceManifestCli` limité à l'unique graphe web.
- **[ ] T12 — Scripts host : `seed-bulk` quitte le binaire web.** Dans `packages/scripts-cs/Program.cs`, ajouter les sous-commandes `seed-bulk` / `seed-bulk-reset` (dispatch identique à `generate-translation-keys` ; `System.CommandLine` peut venir plus tard — l'issue le préfère mais n'impose pas de le faire en même temps), appelant le cœur de seeding déménagé (`Lib/Seeding/BulkSeedConstants/BulkSeedDataGenerator` → `packages/shared-cs/Lib/Seeding/` en T5/T8). Supprimer `BulkSeedCli.cs` de `apps/api` et son dispatch (:63). Mettre à jour `justfile:236-241` : `cd packages/scripts-cs && dotnet run -- seed-bulk`. Vérif : `just seed-bulk-reset && just seed-bulk` sur la base locale ; l'image `runtime` ne contient plus le chemin de code (grep du symbole dans la sortie publish).
- **[ ] T13 — Mort d'`APP_ROLE`.** Retirer `AppRole`/`Role` de Core (l'énumération :18 et le parseur :856 meurent faute de consommateurs) ; retirer les pins : `justfile` (`$APP_ROLE=` des recettes :111/:211/:492/:216/:222/:227/:255/:416), `.env.example` (section :128-137), `.github/workflows/quality-gate.yml` (sed :179 + env :203-229), `apps/front/docker-compose.test.yml:11` (l'ancre lance désormais le binaire web dédié), les deux blocs Dockerfile (retrait de la ligne `APP_ROLE=api \` uniquement ; les autres variables RESTENT — le garde C11 continue d'exiger le jeu complet moins `APP_ROLE`, ré-enseigné en retirant `"APP_ROLE"` de la liste attendue). Fixtures d'intégration : `ApiFixture` compose web+jobs via une composition test explicite dans `Lib/Testing/` (légitime : composition de test, hors production). Recette `test-api` : `$APP_ROLE` supprimé, `$ASPNETCORE_ENVIRONMENT="Testing"` conservé. Vérifs : `just generate-client` vert SANS `APP_ROLE`, `pnpm --filter front typecheck` vert (contrat inchangé → client non régénéré), `just test-api` filtré puis complet.
- **[ ] T14 — Specs de composition par hôte.** Remplacer `AppRoleComposition.Spec` par : `apps/worker/WorkerComposition.Spec.cs` (le graphe ne contient AUCUN type web : pas de `KestrelServer`, pas de middleware ; contient processeur + heartbeat + gate) et `apps/api/ApiComposition.Spec.cs` (zéro hosted-service consommateur de jobs ; endpoints complets). Conserver le motif `HostedServiceManifestCli` côté worker (probe hors HTTP). **Re-confirmer dans ce commit** que la chaîne de migration fonctionne SANS `APP_ROLE` : reconstruire le stage `migrations` (repli `--project packages/shared-cs --startup-project apps/api` câblé en T3) et vérifier la PRÉSENCE du bundle `/publish/efbundle` ; `just db-migrate`/`just db-add` verts via le repli (le plan r1 affirmait « continuent de fonctionner sans APP_ROLE » sans preuve — ici câblé et vérifié).
- **[ ] T15 — CI/deploy-images : préparer (sans basculer) la prod.** Ajouter à `.github/workflows/deploy-images.yml` un 4e build `worker` (nouveau `apps/worker/Dockerfile`, target runtime unique, même pattern de tags SHA). Ne PAS toucher `dokploy.yml` (voir §7). Les images `api`/`migrate`/`front` continuent d'être publiées inchangées tant que la prod n'a pas basculé. **Indépendant de #255 :** cette tâche ne fait que publier une image Docker de plus ; elle ne suppose aucun AppHost.
- **[ ] T16 — `[post-#255]` Intégration AppHost.** **Dépend de #255 :** cette tâche ne peut être exécutée QUE si #255 a livré l'AppHost (`dokploy.yml`/AppHost C#). C'est la seule tâche du plan avec cette dépendance — toutes les autres (T1-T15) produisent des exécutables ordinaires et tournent sans #255 (variantes sans #255 en §1). Ajouter à l'AppHost livré par #255 : ressource `worker` (projet `apps/worker`, plus de double-référence du projet api avec `APP_ROLE`), ressource `api` (projet `apps/api`), PostgreSQL et front tels que #255 les orchestre déjà. Scripts exclus (CLI one-shot). Mettre à jour le comparatif mesuré exigé par #255.

### Phase C — bascule production (PR séparée, décision propriétaire)

Voir §7. Cette phase n'est PAS dans cette lane : elle exige que T1-T15 soient fusionnés, que l'image `worker` existe en GHCR, et l'accord explicite du propriétaire sur le moment du cutover (production en ligne, contrat relu main).

## 5. Non-goels de ce chantier

- Pas de micro-paquetage : UN paquet cœur, pas de `PublyApp.Domain`/`PublyApp.Infrastructure` séparés tant que deux hôtes n'exigent pas la scission.
- Pas de déménagement des préoccupations web hors de `apps/api`.
- Pas de `packages/scripts-js` ici (hors périmètre du re-scope du 2026-08-26 ; suivi séparé).
- Pas d'adoption des ServiceDefaults générés en bloc (contrainte #255 : chemin de logs OTLP non sanitiser interdit).
- Pas de changement de contrat HTTP → aucune régénération Kiota attendue dans toute la phase A/B.

## 6. Risques et points de vigilance nommés

1. **Découverte EF design-time par référence de projet** (T3/T14) : documentée EF Core, mais cassée dès que `AppDbContext`+`Migrations/` quittent `apps/api` (projet de démarrage) pour `packages/shared-cs` (projet non-web, non-démarrage). Le repli est désormais **câblé explicitement** dans T3 (Dockerfile stage `migrations` :64 + recettes justfile `db-add`/`db-migrate` pointant `--project packages/shared-cs --startup-project apps/api`, recette dérivée `db-add-core`/`db-migrate-core`) et **re-vérifié** en T14 (présence du `/publish/efbundle`, `db-migrate`/`db-add` verts SANS `APP_ROLE`). La présence du bundle est une vérification OBLIGATOIRE post-tranche, pas une supposition.
2. **Le garde C11 est adhésif** : toute nouvelle surface qui boot l'app (nouvel hôte !) doit porter le jeu requis — T15/T10 pensent à enseigner le Dockerfile worker au garde si son build boot l'app.
3. **Silence réflexion** (le pire risque du chantier) : un type manquant dans l'assembly du DbContext ne produit aucune erreur — ni seeder peuplé, ni configuration EF appliquée, ni service `[Service]` enregistré (CRITIQUE 2 R2 : `ServiceScanner.ScanAssembly<Program>()` cesse de voir les 43 services Core après T5). Contre-mesure : (a) configs/seeders liés à `typeof(AppDbContext).Assembly` → déjà Core, restent corrects (§3.4 sites 1/2/7) ; (b) services DI → balayage multi-assembly + **`DiServiceRegistration.Completeness.Spec` (§3.4) qui interroge le conteneur réel et compte/name les services attendus** ; (c) après CHAQUE tranche, comparer le décompte des types découverts par réflexion (seeders, configurations) avant/après.
3. **Développe bouge** (il a bougé pendant la rédaction initiale : `…`→`ebdf2b6b6`) : rebaser chaque tâche avant de pousser ; les numéros de ligne du présent plan sont périssables par conception (registre `.dump/citations-r2.md` = vérité datée au tip `ebdf2b6b6`).
4. **Ordre interne de la phase A** : T3 (données+migrations) avant T4 (entités) évite un état intermédiaire où le contexte compile sans ses entités. Ne pas paralléliser T3/T4.

## 7. `dokploy.yml` : ce qu'il FAUDRAIT changer (listé ici, changé nulle part)

Au moment du cutover (phase C, PR dédiée) :
1. `publyapp-worker.image` : `ghcr.io/publyapp/publyapp/api:${RELEASE_TAG}` → `ghcr.io/publyapp/publyapp/worker:${RELEASE_TAG}` (ligne ~120) ; suppression du commentaire « Same GHCR image » devenu faux.
2. `publyapp-worker.healthcheck.test` : `['CMD','dotnet','PublyApp.Api.dll','--worker-health']` → `'PublyApp.Worker.dll'` (ligne ~178) — intervalle/timeouts/start_period INCHANGÉS.
3. `publyapp-migrate` : inchangé (le bundle sort toujours de la chaîne Dockerfile api→migrate ; l'assembly migrations vit dans Core.dll, transparent pour le conteneur).
4. `publyapp-api` : inchangé (image, healthcheck `/health/ready`, `start_period: 300s`).
5. Variables d'environnement : `APP_ROLE=api`/`APP_ROLE=worker` des trois services deviennent mortes → les retirer DANS LA MÊME PR que le cutover, pas avant (sinon l'ancienne image en cours refuse de booter).
6. Runbook : `docs/deployment/production-deploy-runbook.md:21` (« no separate worker image ») réécrit ; `first-deploy-runbook.md` reçoit l'image `worker` dans sa table REQUIRED si le garde C10/C11 l'exige.

## 8. Preuve et traçabilité

- Registre : `.dump/citations-r2.md` (RÉGÉNÉRÉ round 2, **36 citations C1-C36, 0 FAIL**, exécuté réellement contre le tip `ebdf2b6b6517e84b8f052b26cf93568e4c5dc949` (`ebdf2b6b6`) — le même tip que celui cité ici). C1-C31 = état des lieux (mêmes assertions que le r1, désormais PASS toutes, sans ligne FAIL périmée) ; C32-C36 = inventaires R2 (réflexion sur assembly : 14 sites — 13 via `typeof(X).Assembly` / 15 lignes grep + 1 host-agnostique `AppDomain.CurrentDomain.GetAssemblies`, `[Service]` 43, `IEntityTypeConfiguration<T>` 27, précédents Roslyn). L'ancien `.dump/citations-r1.md` est obsolète (tip `6d716e3e6`, 4 lignes FAIL) et ne doit plus être cité.
- Règle tenue : aucun commit tant qu'une ligne du registre est FAIL (aucune ici).
- Section « non vérifié » du corps de PR : énumérée honnêtement (cf. `.dump/pr-body.md`).
