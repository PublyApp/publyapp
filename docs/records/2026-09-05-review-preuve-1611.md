# Revue de preuve — issue #1611 (PR #2105)

## Conception versionnée

La preuve appariée est
`apps/front/tests/proofs/1611/red-1611-deleted-picker-message-collapsed.test.ts`.
Les fichiers de preuve et ce record versionné sont la source de vérité; aucun
`.dump` n’est requis ni consulté.
Elle mute exactement le fichier de production
`apps/front/src/routes/authed/tenant/_tenant-picker-states.tsx` dans un worktree
temporaire isolé :

```diff
- if (hasDeletedTenants) {
+ if (false) {
```

Après cette mutation, la preuve lance le runner versionné
`apps/front/scripts/run-e2e-front.mts` avec les sélecteurs fixes suivants :

```text
E2E_PLAYWRIGHT_SPEC=e2e/tenant-portal-picker.spec.ts
E2E_PLAYWRIGHT_GREP=@1611
E2E_PLAYWRIGHT_PROJECT=chromium
```

Le runner reconstruit le runtime front Docker et rejoue le vrai test
`explains that the active tenant was removed by an administrator` dans le vrai
parcours `tenant-portal-picker`. La preuve exige l’échec muté sur le texte
all-deleted, puis supprime le worktree isolé dans `finally`. Le worktree courant
n’est jamais muté : un watchdog ou SIGKILL ne peut donc pas laisser la source de
la branche dans l’état muté. Le child E2E est détaché, borné à 240 secondes,
terminé par TERM puis KILL; si le leader sort avant ses descendants, la preuve
interroge le groupe POSIX jusqu’à sa disparition et garde une borne de
settling; sous Windows elle utilise `taskkill /T` puis `/T /F` et la même borne.
L’orchestration est versionnée dans
`apps/front/scripts/run-proof-child.mts`; son test injecte un faux child, des
timers, la sonde de groupe et le signaleur, et vérifie que l’`exit` du leader ne
résout pas avant l’envoi de SIGKILL au descendant résistant.
Le test Vitest est borné à 270 secondes. Aucun
`Response`, composant, page, module i18n ou route de preuve injecté n’est
utilisé.

Le vert est une exécution distincte, hors de ce test Vitest, sur la source
non-mutée :

```text
E2E_PLAYWRIGHT_SPEC=e2e/tenant-portal-picker.spec.ts \
E2E_PLAYWRIGHT_GREP=@1611 \
E2E_PLAYWRIGHT_PROJECT=chromium \
node apps/front/scripts/run-e2e-front.mts
```

Le manifest versionné
`red-1611-deleted-picker-message-collapsed.test.ts.expected-red.json` porte le
nom exact :
`the real @1611 Playwright journey rejects collapsed all-deleted copy`.

## Nettoyage E2E

La suppression réussie du parcours est mémorisée par `tenantDeleted`; le
`finally` n’envoie donc pas un second POST suspend sur un tenant déjà soft-delete.
Ce second POST était la cause racine du timeout de 30 s observé à
`tenant-account-fixture.ts:387` et il masquait l’erreur produit quand le
`finally` lançait à son tour. Les appels de cleanup restants sont bornés à
5 secondes; la commande SQL Docker est elle aussi bornée à 10 secondes, le
passage SQL reste toujours exécuté, et les erreurs du parcours et du cleanup
sont agrégées sans être avalées.

Si la création répond 201 avec un body invalide ou sans `id`, le cleanup
recherche exactement un tenant persistant par l’email aléatoire connu dans
Postgres, puis applique le même cleanup tenant+utilisateur. Zéro ou plusieurs
IDs sont traités comme une mesure impossible, jamais comme une autorisation de
nettoyage large.

## Mutations adverses nommées

| Mutation | Mécanisme distinct | Test rouge attendu |
| --- | --- | --- |
| `if (hasDeletedTenants)` → `if (false)` dans `_tenant-picker-states.tsx` | branche de rendu production; replay Docker primaire, non lancé ici | `the real @1611 Playwright journey rejects collapsed all-deleted copy` |
| `apps/front/src/lib/query/tenants-for-picker.ts`: `result?.hasDeletedTenants ?? false` → `false` | signal API → route; **exécutée**, exit 1 (`2 failed`, `7 passed`) | `toTenantsForPickerData > maps hasDeletedTenants through for an otherwise-empty picker` |
| `apps/front/src/routes/authed/tenant/_tenant-picker-states.tsx`: titre all-deleted → `t('no-organizations-found')` | copie de titre ambiguë; **exécutée**, exit 1 (`1 failed`, `18 passed`) | `TenantPortalRoute > #258: renders the deletion notice when every tenant was soft-deleted` |
| même fichier: description all-deleted → `t('no-organizations-found')` | copie de description ambiguë; **exécutée**, exit 1 (`1 failed`, `18 passed`) | `TenantPortalRoute > #258: renders the deletion notice when every tenant was soft-deleted` |

Les trois mutations supplémentaires ont donc été réellement plantées dans un
worktree temporaire, chacune a rendu son test non-Docker rouge, puis le
worktree a été restauré et supprimé. Elles réintroduisent toutes l’ambiguïté
entre « toutes supprimées » et « jamais invité » par des mécanismes distincts
(signal, titre, description). Une mutation de logout n’est pas comptée : elle
teste une action, pas l’ambiguïté d’état/copie demandée. Le replay Docker de la
mutation primaire et le vert réel restent réservés au capitaine; aucune
exécution Docker n’est présentée comme preuve locale.

## Vérification locale légère

Les vérifications sans Docker produisent le résumé vert suivant :

```text
Vitest: Tests 47 passed/47 total
Node launch-shape/tree-termination: Tests 18 passed/18 total
Node proof-child orchestration: Tests 1 passed/1 total
Oxlint ciblé: passed
Oxfmt ciblé: 3 files matched
Front typecheck + coverage guard: 83 expected artifacts present [OK]
```

Commande exacte :

```text
pnpm --filter front exec vitest run \
  e2e/helpers/tenant-account-fixture.test.ts \
  scripts/ci/run-proofs.test.ts \
  src/routes/authed/tenant/_tenant-picker-states.test.tsx \
  src/routes/authed/tenant.test.tsx
node --test scripts/run-e2e-front.launch.test.mts
node --test scripts/run-proof-child.test.mts
pnpm --filter front exec oxlint --deny-warnings \
  scripts/run-e2e-front.mts scripts/run-e2e-front.launch.test.mts \
  scripts/run-proof-child.mts scripts/run-proof-child.test.mts \
  tests/proofs/1611/red-1611-deleted-picker-message-collapsed.test.ts
pnpm --filter front exec oxfmt --check \
  scripts/run-e2e-front.mts scripts/run-e2e-front.launch.test.mts \
  scripts/run-proof-child.mts scripts/run-proof-child.test.mts \
  tests/proofs/1611/red-1611-deleted-picker-message-collapsed.test.ts
pnpm --filter front typecheck
```

La commande de preuve appariée Docker elle-même n’est pas lancée localement,
car elle construit et démarre Docker par conception et le verrou lourd de
#2104 est actif.
