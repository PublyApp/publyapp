# Revue de preuve — issue #1611 (PR #2105)

## Conception versionnée

La preuve appariée est
`apps/front/tests/proofs/1611/red-1611-deleted-picker-message-collapsed.test.ts`.
Les fichiers de preuve et ce record versionné sont la source de vérité; aucun
`.dump` n’est requis ni consulté.
Elle mute exactement le fichier de production
`apps/front/src/routes/authed/tenant/_tenant-picker-states.tsx` :

```diff
- if (hasDeletedTenants) {
+ if (false) {
```

Après cette mutation, la preuve lance le runner versionné
`apps/front/scripts/run-e2e-front.mts` avec les sélecteurs fixes suivants :

```text
E2E_PLAYWRIGHT_SPEC=e2e/tenant-portal-picker.spec.ts
E2E_PLAYWRIGHT_GREP=@1611
E2E_PLAYWRIGHT_PROJECT=chromium
```

Le runner reconstruit le runtime front Docker et rejoue le vrai test
`explains that the active tenant was removed by an administrator` dans le vrai
parcours `tenant-portal-picker`. La preuve exige l’échec muté sur le texte
all-deleted, restaure le fichier dans `finally`, puis exige le succès du même
parcours restauré. Aucun `Response`, composant, page, module i18n ou route de
preuve injecté n’est utilisé.

Le manifest versionné
`red-1611-deleted-picker-message-collapsed.test.ts.expected-red.json` porte le
nom exact :
`the real @1611 Playwright journey rejects collapsed all-deleted copy`.

## Nettoyage E2E

La suppression réussie du parcours est mémorisée par `tenantDeleted`; le
`finally` n’envoie donc pas un second POST suspend sur un tenant déjà soft-delete.
Ce second POST était la cause racine du timeout de 30 s observé à
`tenant-account-fixture.ts:387` et il masquait l’erreur produit quand le
`finally` lançait à son tour. Les appels de cleanup restants sont bornés à
5 secondes; la commande SQL Docker est elle aussi bornée à 10 secondes, le
passage SQL reste toujours exécuté, et les erreurs du parcours et du cleanup
sont agrégées sans être avalées.

Si la création répond 201 avec un body invalide ou sans `id`, le cleanup
recherche exactement un tenant persistant par l’email aléatoire connu dans
Postgres, puis applique le même cleanup tenant+utilisateur. Zéro ou plusieurs
IDs sont traités comme une mesure impossible, jamais comme une autorisation de
nettoyage large.

## Mutations adverses nommées

| Mutation | Mécanisme distinct | Test rouge attendu |
| --- | --- | --- |
| `if (hasDeletedTenants)` → `if (false)` dans `_tenant-picker-states.tsx` | branche de rendu production | `the real @1611 Playwright journey rejects collapsed all-deleted copy` |
| `hasDeletedTenants` forcé à `false` dans la propagation de la réponse picker | signal API → route | `explains that the active tenant was removed by an administrator` |
| retrait de `tenant-portal-logout-button` de la branche all-deleted | action DOM de sortie | `explains that the active tenant was removed by an administrator` |

Ces trois mutations sont des mécanismes indépendants; chacune doit rendre le
parcours réel rouge sur une assertion différente (signal, copy, action). Le
replay Docker de ces mutations et la paire rouge/verte complète sont réservés
au runner E2E du capitaine dans cette passe; aucune exécution Docker n’est
présentée comme preuve locale.

## Vérification locale légère

Les vérifications sans Docker produisent le résumé vert suivant :

```text
Vitest: Tests 47 passed/47 total
Node launch-shape: Tests 17 passed/17 total
```

Commande exacte :

```text
pnpm --filter front exec vitest run \
  e2e/helpers/tenant-account-fixture.test.ts \
  scripts/ci/run-proofs.test.ts \
  src/routes/authed/tenant/_tenant-picker-states.test.tsx \
  src/routes/authed/tenant.test.tsx
node --test scripts/run-e2e-front.launch.test.mts
```

La commande de preuve appariée elle-même n’est pas lancée localement, car elle
construit et démarre Docker par conception.
