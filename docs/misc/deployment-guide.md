# Deployment Guide (Legacy)

This document used to describe an image-based Dokploy flow using repo Dockerfiles and a `scripts/build-and-push.mjs` helper.

Those files were intentionally removed.

Use the current “local build → upload artifacts” pipeline instead:
- `docs/misc/dokploy-artifact-pipeline.md`

