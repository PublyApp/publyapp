// Scratch file for the paired proof required by #1270: a deliberate type
// error that the shared-ts gate must catch. Deleted in the very next commit.
export const scratchProof = (n: number): string => n;
