name: quality gate

# Fails PRs on (a) any oxlint diagnostic (repo-wide pnpm lint + oxfmt check) and
# (b) any .NET analyzer / code-style warning (TreatWarningsAsErrors +
# EnforceCodeStyleInBuild from Directory.Build.props, so a restore+build of
# PublyApp.slnx with those props active is the gate) plus custom analyzer tests.
# See issue #803.

on:
  pull_request:
  # Required so this required check can report for a merge-queue entry: a
  # required Actions check missing this event waits forever once the entry
  # reaches the queue (GitHub's documented behavior), reproducing the exact
  # missing-check deadlock #1017 exists to remove — just under a merge queue
  # instead of an unfiltered `pull_request` trigger. The repository has no
  # merge-queue rule today, so this is dormant, but a required check that
  # cannot report under a supported GitHub feature is a latent version of
  # that bug.
  merge_group:
  push:
    branches: [develop]
    paths:
      - '.github/workflows/quality-gate.yml'
      - '.oxlintrc.json'
      - '.oxfmtrc.json'
      - 'package.json'
      - 'knip.ts'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'turbo.json'
      - '.npmrc'
      - 'Directory.Build.props'
      - 'Directory.Build.targets'
      - 'Directory.Packages.props'
      - 'global.json'
      - 'PublyApp.slnx'
      - 'apps/**'
      - 'packages/**'
      - 'packages/scripts-ts/**'
      - '.gitignore'
      # #1513: the no-ignored-tracked guard itself and the .gitignore it
      # enforces — a new gitignored surface or a change to the guard must
      # wake the gate.
      - 'packages/scripts-ts/src/check-no-ignored-tracked.ts'
      - 'packages/scripts-ts/src/check-no-ignored-tracked.test.ts'
      # #1821: the jscpd ratchet and its reference + tests.
      - 'packages/scripts-ts/src/check-jscpd.ts'
      - 'packages/scripts-ts/src/check-jscpd.test.ts'
      - 'packages/scripts-ts/src/jscpd-reference.json'
      # #1334 fix round 1: the dependency-health.md pin-location contract
      # (packages/scripts-ts/src/dependency-health-pin-location.test.ts) runs in
      # this workflow, so a pull request touching ONLY the guarded doc must
      # still wake the gate — otherwise the sentence could drift straight past
      # the contract test that exists to catch it.
      - 'docs/guides/dependency-health.md'
      - 'docs/deployment/first-deploy-runbook.md'

permissions:
  contents: read

concurrency:
  group: quality-gate-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Cheap first job: the pull_request trigger above has no `paths:` filter (a
  # path-filtered trigger never starts for an unrelated PR, so a required
  # status check gated only by the trigger would hang forever — see #1017).
  # This job always runs and decides whether the expensive job below should.
  changes:
    name: Determine changed paths
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      relevant: ${{ steps.filter.outputs.relevant }}
    steps:
      # Checked out from the BASE commit, not this pull request's own ref:
      # this pins the classifier a pull request is being evaluated against so
      # an accidental or careless edit (a rename, a bad rebase, editing the
      # wrong copy) cannot silently change the relevance decision.
      #
      # This is NOT a defense against a pull request that deliberately tries
      # to bypass its own gates. The `changes` job's YAML — including the
      # invocation step below, and this comment — is itself read from the
      # pull request's merge commit, the same as every other job in this
      # workflow. A pull request can already rewrite that step's `run:` to
      # print `relevant=true`/`false` unconditionally, or delete this
      # checkout, or anything else, exactly as freely as it could edit a
      # separate script file. Pinning the classifier's SOURCE closes the
      # narrower "an edit to ci-changed-paths.mjs itself is silently
      # honored" gap; it does not and cannot close the wider gap that the
      # workflow calling it is merge-commit-controlled. That wider gap is
      # tracked in #1022, with the honest framing: these gates defend against
      # mistakes, not a malicious author of a branch in this repository — the
      # real controls there are who can push branches and GitHub's
      # fork-approval requirement.
      #
      # This checkout can legitimately produce nothing: the pull request that
      # introduces this classifier (or a branch cut before it existed on the
      # base branch) has no such file at its base commit. The invocation step
      # below fails closed to relevant=true rather than crashing when that
      # happens.
      - name: Checkout classifier from the base commit (not the PR's own copy)
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.sha }}
          sparse-checkout: |
            packages/scripts-ts/src/ci-changed-paths.ts
          sparse-checkout-cone-mode: false
          path: base-ref

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24

      - name: Check whether quality-gate paths changed
        id: filter
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        # Mirrors the push paths above. The classifier itself
        # (packages/scripts-ts/src/ci-changed-paths.ts) fails closed to relevant=true when
        # the changed-file list can't be verified complete (GitHub's
        # 3,000-file API ceiling) rather than trusting a truncated list — see
        # packages/scripts-ts/src/ci-changed-paths.test.ts.
        #
        # The base-pinned checkout above can legitimately be empty (see its
        # comment). If so, this fails closed to relevant=true on a path that
        # never invokes the classifier at all, kept structurally separate
        # from the classifier's own answer so a real relevant=false can never
        # be mistaken for "missing" and vice versa. See
        # packages/scripts-ts/src/ci-gate-bootstrap.test.ts, which executes this exact
        # inline script (parsed from this file) against both cases.
        run: |
          CLASSIFIER=base-ref/packages/scripts-ts/src/ci-changed-paths.ts

          if [ -f "$CLASSIFIER" ]; then
            node "$CLASSIFIER" '^(\.github/workflows/quality-gate\.yml$|\.oxlintrc\.json$|\.oxfmtrc\.json$|package\.json$|knip\.ts$|pnpm-lock\.yaml$|pnpm-workspace\.yaml$|turbo\.json$|\.npmrc$|Directory\.Build\.props$|Directory\.Build\.targets$|Directory\.Packages\.props$|global\.json$|PublyApp\.slnx$|\.gitignore$|apps/|packages/|docs/guides/dependency-health\.md$|docs/deployment/first-deploy-runbook\.md$)'
          else
            echo "::warning::[MISSING BASE CLASSIFIER] $CLASSIFIER does not exist at the base commit. This is expected exactly once: for the pull request that introduces this classifier, or a branch cut before it existed on the base branch. Relevance cannot be determined from a classifier that is not there, so this fails closed: treating the workflow as relevant and running everything. If this keeps happening on ordinary pull requests well after the classifier has been on the base branch, something is wrong — investigate, do not silence it."
            echo "relevant=true" >> "$GITHUB_OUTPUT"
          fi

  quality:
    needs: changes
    if: needs.changes.outputs.relevant == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Install pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10
        with:
          version: 10.13.1

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24

      - name: Setup .NET (pinned for CI determinism)
        uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6.0.0
        with:
          dotnet-version: "10.0.102"

      - name: Setup just
        uses: extractions/setup-just@53165ef7e734c5c07cb06b3c8e7b647c5aa16db3 # v4.0.0

      - name: Assert exact pins
        run: node apps/front/scripts/guards/assert-pinned.mts

      - name: Install (frozen lockfile, no scripts)
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Run trusted first-party postinstall (@org/shared-ts)
        run: pnpm --filter @org/shared-ts run postinstall

      # Build-time OpenAPI generation boots the app (host env unset -> Production),
      # so AppEnvironment.Initialize() loads the repo-root .env.development for its
      # config values. That file is gitignored and absent in CI, so materialize it
      # from the committed .env.example template. See openapi-spec-drift.yml for
      # the same pattern and the defense-in-depth comment on APP_ROLE pinning.
      - name: Materialize .env.development from template (CI has no gitignored env file)
        run: |
          cp .env.example .env.development
          sed -i 's/^APP_ROLE=.*/APP_ROLE="api"/' .env.development

      - name: Check formatting (oxfmt --check)
        run: pnpm format

      # #1875: the formatter-scope guard. `pnpm format` above runs oxfmt with
      # the globs declared in package.json against the ignorePatterns in
      # .oxfmtrc.json. The guard asks OXFMT ITSELF which files each glob would
      # process, so an ignorePatterns entry that swallows a directory the globs
      # explicitly enumerate (the #1875 bypass) turns this step red naming the
      # directory, instead of silently skipping the files. Mirrored by
      # `just ci-format` (the same vitest file runs inside the recipe).
      - name: Check formatter scope (#1875)
        run: pnpm --filter scripts-ts exec vitest run src/check-formatter-scope.test.ts

      - name: Lint (oxlint repo-wide)
        run: pnpm lint

      - name: Verify complexity bounds are not drifting (#1661)
        run: node packages/scripts-ts/src/check-cyclomatic-bound.ts
      # #1821: production duplication ratchet. Runs the local gate
      # (`just ci-jscpd`): full-tree jscpd scan with the single comma-separated
      # exclusion list, then the guard's unit suite (vitest), then the ratchet
      # guard itself, then the guard's typecheck. The manifest entry for this
      # step mirrors `just ci-jscpd` exactly — the local command and the CI step
      # are the same four commands in the same order.
      - name: Verify production duplication has not increased (#1821)
        run: just ci-jscpd

      - name: Lint plugin unit tests (lint-ts)
        run: pnpm --filter @org/lint-ts test

      # @org/lint-ts ships the repo's custom publy/* oxlint rules that guard
      # every front surface, but nothing verified its own types — only its
      # vitest tests ran (the step above). The typecheck script now runs here
      # exactly as `just ci-lint-ts` runs it; see issue #1600.
      - name: Typecheck lint-ts
        run: pnpm --filter @org/lint-ts typecheck

      # @org/client-ts is the Kiota-generated TypeScript API client. Its
      # typecheck script (tsc --noEmit) was added but never wired into any gate
      # — a typecheck script no CI step calls is trompe-l'oeil coverage. This
      # step runs it exactly as `just ci-lint-ts` runs it locally; see issue #1692.
      - name: Typecheck client-ts
        run: pnpm --filter @org/client-ts typecheck

      # #455: fail on unused files/deps/exports/types and duplicate exports.
      # Mirrored locally by `just ci-knip` (same command, same knip.ts).
      - name: Knip (unused exports & dependencies)
        run: pnpm exec knip

      # @org/shared-ts ships the repo-wide logger, i18n, query-factory and
      # ApiFailure contracts every front surface imports, but nothing standing
      # verified it after its i18next range moved two majors (#1262). Both
      # commands run here exactly as `just ci-shared-ts` runs them locally;
      # see issue #1270.
      - name: Typecheck shared-ts
        run: pnpm --filter @org/shared-ts typecheck

      - name: Test shared-ts
        run: pnpm --filter @org/shared-ts test

      - name: Restore .NET solution (PublyApp.slnx)
        env:
          APP_ROLE: api
          TRUSTED_PROXY_CIDRS: "127.0.0.1/32"
          SOCIAL_ACCOUNTS_MASTER_KEY: "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA="
        run: dotnet restore PublyApp.slnx

      - name: Build .NET solution (warnings as errors via Directory.Build.props)
        env:
          APP_ROLE: api
          TRUSTED_PROXY_CIDRS: "127.0.0.1/32"
          SOCIAL_ACCOUNTS_MASTER_KEY: "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA="
        run: dotnet build PublyApp.slnx --no-restore

      # NuGet vulnerability scan (issue #1187 rung 3). Uses the machine-readable
      # `--format json` output (not the text format that TreatWarningsAsErrors
      # breaks by converting NU1903 into an error before the grep pattern can
      # match). Scans EVERY .csproj in the repo (via `git ls-files`), not just
      # PublyApp.slnx: the slnx omits packages/lint-cs/*, but the audit scope
      # covers all five projects. Mirrors `just nuget-audit`.
      - name: Scan .NET packages for known vulnerabilities
        env:
          APP_ROLE: api
          TRUSTED_PROXY_CIDRS: "127.0.0.1/32"
        run: node packages/scripts-ts/src/nuget-audit.ts

      # #1631: the production-only `pnpm audit --prod --audit-level=high`
      # (front-ci.yml::supply-chain) never inspects the development dependency
      # graph, so a vulnerable dev-only transitive (e.g. nanoid via the dev
      # dependency postcss) resolves green while the repo stays exposed behind a
      # hand-written pnpm.overrides. This scans the dev-only graph repo-wide (no
      # --filter, so every package.json's dev deps count) at `moderate` — stricter
      # than production's `high`, because a moderate dev-graph advisory must turn
      # the gate red rather than be silently skipped. The graph is currently clean
      # at every threshold (0 advisories at critical/high/moderate/low — see
      # .dump/preuve-1631.md), so no named exception list is needed yet.
      - name: Audit development dependencies (moderate and above)
        run: pnpm audit --dev --audit-level=moderate

      - name: Run NuGet audit fixture tests
        env:
          APP_ROLE: api
          TRUSTED_PROXY_CIDRS: "127.0.0.1/32"
        run: pnpm --filter scripts-ts exec vitest run src/nuget-audit.test.ts

      # #1334 fix round 1 (review r1 MAJOR): this contract test pins the
      # dependency-health.md sentence that says WHERE the patched
      # @microsoft/kiota-http-fetchlibrary pin lives against the real
      # manifests. It shipped unwired in #1334's first push, so it never
      # executed in CI and the doc could still have drifted past it — an
      # orphaned test is a claim, not a guard. Runs here next to the other
      # scripts-ts contract test (nuget-audit.test.ts); mirrored locally by
      # `just nuget-audit`.
      - name: Run dependency-health pin-location contract test
        run: pnpm --filter scripts-ts exec vitest run src/dependency-health-pin-location.test.ts

      # #1392 supply-chain binding: every 40-hex action SHA must be the commit
      # its "# vX.Y.Z" comment claims. Resolves each tag through the GitHub API
      # (annotated tags peeled to their commit) and compares; unparseable pins
      # (no comment, non-version comment, short/mutable ref) fail loud. Mirrors
      # `just ci-actions-pins`, whose ARGS="--offline" skip exists for local
      # air-gapped runs only — CI always executes the live half below.
      - name: Bind pinned action SHAs to their version comments (#1392)
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          pnpm --filter scripts-ts exec vitest run src/check-actions-pins.test.ts
          node ./packages/scripts-ts/src/check-actions-pins.ts

      # #1798: every production-required env var documented in the deploy runbook.
      # Extracts required vars from actual source code (env.ts schema, server.ts
      # validateRuntimeEnv, AppEnvironment.cs GetRequiredString calls) and the
      # documented vars from the actual runbook (§5a table + §5b block). Fails
      # closed: an unparseable source file FAILS LOUDLY with the file name and
      # error rather than silently passing. Mirrors `just ci-deploy-env-docs`.
      - name: Verify deploy env doc coverage (#1798)
        run: |
          pnpm --filter scripts-ts exec vitest run src/check-deploy-env-docs.test.ts
          node ./packages/scripts-ts/src/check-deploy-env-docs.ts

      - name: Run analyzer tests
        run: pnpm exec just test-analyzers

  # #1513: unconditioned guard — no tracked file may match a .gitignore rule.
  #
  # WHY THIS IS A SEPARATE, UNCONDITIONED JOB
  # ----------------------------------------
  # This guard interrogates the real repository via
  # `git ls-files --cached --ignored --exclude-standard`. Its purpose is to
  # catch force-added gitignored files (e.g. `git add -f .dump/...`). By
  # construction, the files it is designed to catch live on surfaces that are
  # gitignored — surfaces that the changed-paths classifier below does not
  # and CANNOT enumerate without becoming a hand-maintained list that rots
  # the instant a new gitignored surface is added.
  #
  # Gating this guard behind `needs.changes.outputs.relevant == 'true'` is a
  # logical contradiction: a classifier of classified paths can never
  # recognize the arrival of a file whose defining property is that it is on
  # an UNCLASSIFIED path. A PR that adds a `.dump/` file without touching any
  # classified path is declared "not relevant", the heavy job is skipped,
  # and this guard — the one check whose entire reason for existence is to
  # catch exactly that scenario — never runs. The local `just ci` gate
  # catches it; CI does not. The guard looks present and protects nothing.
  #
  # This job runs UNCONDITIONALLY (no `if:`), so it always reports. It
  # executes only `git ls-files` — no install, no build — so it is cheap and
  # fast. Mirrors `just ci-no-ignored-tracked` (which runs the same script
  # plus its vitest test).
  no-ignored-tracked:
    name: No tracked file matches .gitignore (#1513)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Verify no tracked file matches .gitignore (#1513)
        run: node packages/scripts-ts/src/check-no-ignored-tracked.ts

  # #1849: no `<Dockerfile>.dockerignore` shadow file may exist anywhere in
  # the tree — Docker REPLACES (not merges) the root .dockerignore when one
  # sits next to a Dockerfile, silently re-including node_modules, dist,
  # .turbo and .worktrees in every build context (see #1832/#1836).
  #
  # WHY THIS IS A SEPARATE, UNCONDITIONED JOB
  # -----------------------------------------
  # Same argument as no-ignored-tracked above: the guard scans for files that
  # could arrive on ANY path (`Dockerfile.dockerignore` next to any current or
  # future Dockerfile), and the changed-paths classifier only enumerates the
  # paths it knows today. A shadow file placed on an unclassified path
  # (repo root, docs/, ...) would declare the PR "not relevant", skip the
  # heavy job, and never run the one guard whose reason to exist is to catch
  # exactly that. The local `just ci` gate catches it; this unconditioned job
  # runs the real binary on the real checkout in CI too. Mirrors
  # `just ci-dockerignore-shadow` (which runs the same script plus its
  # fixture suite).
  no-dockerignore-shadow:
    name: Forbid .dockerignore shadow files (#1849)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Forbid .dockerignore shadow files (#1849)
        run: node packages/scripts-ts/src/check-dockerignore-shadow.ts

  # #1891: a `.dockerignore` placed anywhere other than the repository root
  # creates the same context divergence as the shadow file #1849 names — INERT
  # when context = repo root, ACTIVE when context = the subdirectory itself
  # (BuildKit additive sub-context feature). Same rationale for an
  # unconditional job as no-dockerignore-shadow above: the guard scans every
  # subdirectory and the changed-paths classifier cannot enumerate them all.
  # Mirrors `just ci-no-subdir-dockerignore` (which runs the same script plus
  # its fixture suite).
  no-subdir-dockerignore:
    name: Forbid .dockerignore outside the repo root (#1891)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Forbid .dockerignore outside the repo root (#1891)
        run: node packages/scripts-ts/src/check-no-subdir-dockerignore.ts

  # Required status check: this is the one context that should be required
  # on the branch ruleset instead of relying on the trigger. It always
  # reports (if: always()), passes when upstream jobs succeeded or were
  # skipped (irrelevant PR), and fails on any real failure or cancellation
  # rather than treating either as "not applicable".
  gate:
    # See front-ci.yml / openapi-spec-drift.yml gate jobs for the full
    # rationale on the event-conditional `name:` (round 5/6 BLOCKER fixes).
    # `if:` stays unconditional `always()` so a push-triggered run still
    # aggregates a real pass/fail; the colliding-name nondeterminism is
    # closed by `name:` instead. The allowlist form
    # `(pull_request || merge_group) && required || push-check` means any
    # future event added to `on:` resolves to the non-required name by
    # default.
    # packages/scripts-ts/src/check-ci-gate-structure.ts pins this exact expression, caps
    # this workflow's `on:` to pull_request/merge_group/push, and scans
    # every job in every workflow so no other job can report one of the
    # required names either.
    name: ${{ (github.event_name == 'pull_request' || github.event_name == 'merge_group') && 'quality-gate' || 'quality-gate-push-check' }}
    if: always()
    needs: [changes, quality, no-ignored-tracked, no-dockerignore-shadow, no-subdir-dockerignore]
    runs-on: ubuntu-latest
    steps:
      # Reads the `needs` context directly (via toJSON) instead of a
      # hand-written per-job map: a job added to `needs` above without also
      # being added to a hand-maintained Bash list would silently never be
      # checked (round-2 review). `needs` is populated by Actions from this
      # job's own `needs:` keyword, so there is no second list to omit an
      # entry from.
      - name: Check required jobs
        id: check-required-jobs
        env:
          NEEDS_JSON: ${{ toJSON(needs) }}
        # Fail closed on anything outside the exact documented shape: a
        # nonempty JSON object whose every entry's result is exactly
        # "success" or "skipped" (GitHub documents only
        # success/failure/cancelled/skipped for needs.<job>.result — see
        # packages/scripts-ts/src/ci-gate-aggregation.test.ts). An empty object/array, a
        # missing `.result`, or an unrecognized result value (a documented
        # future addition, a typo, a job whose result GitHub reports in a
        # shape this was not written against) must never silently pass
        # "nothing to flag" — and neither may examining zero jobs.
        #
        # BLOCKER fix: treating every "skipped" result as a pass — the
        # original shape of this check — let a review round prove `changes`
        # succeeding with an absent/empty `relevant` output plus every heavy
        # job skipped as green: skipped work was never correlated with the
        # classifier's own answer, so "skipped" was accepted as proof of
        # nothing. This step now also asserts `needs.changes.result ==
        # "success"`, requires `needs.changes.outputs.relevant` to be
        # EXACTLY the string "true" or "false" (GitHub can omit a job output
        # entirely — including because it may contain a secret — so an
        # absent/empty/malformed value fails closed rather than being read as
        # a green light), and only allows a required job to be "skipped" when
        # relevant is exactly "false". When relevant is "true", every
        # required job (including one whose skip was actually caused by an
        # upstream failure cascading a default skip) must be "success".
        run: |
          if [ "$(echo "$NEEDS_JSON" | jq -r 'type')" != "object" ]; then
            echo "::error::NEEDS_JSON is not a JSON object — examining nothing must never pass."
            exit 1
          fi

          if [ "$(echo "$NEEDS_JSON" | jq 'length')" -eq 0 ]; then
            echo "::error::NEEDS_JSON has no entries — examining zero jobs must never pass."
            exit 1
          fi

          echo "Required jobs and their results:"
          echo "$NEEDS_JSON" | jq -r 'to_entries[] | "  \(.key): \(.value.result)"'

          changes_result="$(echo "$NEEDS_JSON" | jq -r '.changes.result // "MISSING"')"

          if [ "$changes_result" != "success" ]; then
            echo "::error::needs.changes.result must be exactly \"success\" (found: \"$changes_result\"). Nothing downstream can be trusted if the changed-paths classifier itself did not run to completion."
            exit 1
          fi

          relevant="$(echo "$NEEDS_JSON" | jq -r '.changes.outputs.relevant // "MISSING"')"

          if [ "$relevant" != "true" ] && [ "$relevant" != "false" ]; then
            echo "::error::needs.changes.outputs.relevant must be exactly the string \"true\" or \"false\" (found: \"$relevant\"). GitHub can omit a job output entirely — including because it may contain a secret — so an absent, empty, or otherwise malformed relevance signal must never be treated as license to accept skipped verification work."
            exit 1
          fi

          if [ "$relevant" = "true" ]; then
            bad_count="$(echo "$NEEDS_JSON" | jq '[.[] | select(.result != "success")] | length')"

            if [ "$bad_count" -gt 0 ]; then
              echo "::error::The changes classifier reported this change as relevant, so every required job must have actually run and succeeded (skipped is not acceptable) — but one or more did not (see results above)."
              exit 1
            fi
          else
            bad_count="$(echo "$NEEDS_JSON" | jq '[.[] | select((.result == "success" or .result == "skipped") | not)] | length')"

            if [ "$bad_count" -gt 0 ]; then
              echo "::error::One or more required jobs did not report success or skipped (failure, cancelled, or an unrecognized result value)."
              exit 1
            fi
          fi

          echo "All required jobs passed, or were correctly skipped for a verified-irrelevant change."

      # BLOCKER fix (round 5): `continue-on-error: true` added to the step
      # above would make its reported CONCLUSION "success" even after it
      # printed `::error::` and exited 1 — masking a correctly-detected
      # aggregation failure the same way it masks a verification step's
      # failure. GitHub computes a step's `outcome` BEFORE continue-on-error
      # is applied (only `conclusion` is rewritten afterward), so reading
      # `steps.check-required-jobs.outcome` here is not fooled by
      # continue-on-error on that step. `if: always()` so this still runs
      # (and still fails the job) even if the step above failed for real
      # without continue-on-error. packages/scripts-ts/src/check-ci-gate-structure.ts
      # separately hard-rejects continue-on-error anywhere in this job, as
      # an independent second layer.
      - name: Verify the required-jobs check truly succeeded (continue-on-error cannot mask this)
        if: always()
        run: |
          outcome="${{ steps.check-required-jobs.outcome }}"

          if [ "$outcome" != "success" ]; then
            echo "::error::steps.check-required-jobs.outcome was \"$outcome\", not \"success\". continue-on-error can rewrite a step's CONCLUSION but not its OUTCOME, so a masked required-jobs check failure is still caught here."
            exit 1
          fi
