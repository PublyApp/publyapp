## Summary

<!-- What changed and why. -->

## Verification

<!-- Commands run and their results (e.g. `just build-api`, `just tsc-front`, `just test-analyzers`). -->

---

Closes #

<!--
REQUIRED: every PR must link a GitHub issue with a closing keyword
(Closes #N / Resolves #N / Fixes #N). The `require-linked-issue` CI check
blocks merge when no issue is linked. Search existing open issues first —
many slices already have an issue that just needs linking. Create one if not.
-->
