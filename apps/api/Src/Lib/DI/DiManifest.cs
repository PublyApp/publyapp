namespace MainApi.Src.Lib.DI;

/// <summary>
/// Holds a formatted DI manifest for logging after the host logging pipeline is configured.
/// </summary>
public sealed record DiManifest(string Text);

