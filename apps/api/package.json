{
	"name": "api",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev": "dotnet watch --project MainApi.csproj",
		"dev:api": "dotnet watch --project MainApi.csproj",
		"build": "dotnet build MainApi.csproj",
		"publish": "dotnet publish MainApi.csproj --output ./bin",
		"migrate:add": "dotnet tool run dotnet-ef migrations add",
		"migrate:apply": "dotnet tool run dotnet-ef database update",
		"migrate:remove": "dotnet tool run dotnet-ef migrations remove"
	}
}
