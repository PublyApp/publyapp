
# Playwright artifacts
test-results/
playwright-report/
# Captured session state (real cookies) from e2e/auth.setup.ts — never commit
# a live session, even a test one.
playwright/.auth/

# TanStack Start build scratch — never commit
.tanstack/

# Vitest JUnit reports (CI artifact)
vitest-results/
