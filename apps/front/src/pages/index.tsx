import { Typography } from '@mui/material';

const Home = () => {
	return (
		<>
			<Typography variant="h1">Hello Dude</Typography>
			<Typography variant="h3">Welcome home</Typography>
		</>
	);
};

export default Home;
