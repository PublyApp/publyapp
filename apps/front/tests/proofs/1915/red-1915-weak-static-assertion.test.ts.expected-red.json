{
	"$schema": "../expected-red.schema.json",
	"_comment": "Per-test kept-red expectation manifest for issue #1915. Both declared tests MUST fail with AssertionErrors on the corrected assertions. If either declared test passes, classifyProofWithManifest returns DECLARED RED PASSED and the runner marks the corresponding proof stale and exits non-zero.",
	"expectedRed": [
		{
			"testName": "normal static-file assertions accept a broken 500 response (#1915 status axis)",
			"why": "The proof executes the real static handler, replaces its successful response with a 500 response carrying the exact body, and passes it to the normal assertions. The corrected status assertion rejects it, so the kept-red test fails with AssertionError. If this test passes, the status validation was removed or weakened and the paired proof must be rejected as stale."
		},
		{
			"testName": "normal static-file assertions accept a successful empty body (#1915 body axis)",
			"why": "The proof executes the real static handler, replaces its successful response body with an empty body while retaining a successful status, and passes it to the normal assertions. The corrected exact-body assertion rejects it, so the kept-red test fails with AssertionError. If this test passes, the exact body validation was removed or weakened and the paired proof must be rejected as stale."
		}
	],
	"_evidence": {
		"replay": "cd apps/front && pnpm exec vitest run --config vitest.proofs.config.ts tests/proofs/1915/red-1915-weak-static-assertion.test.ts",
		"expectedFailure": "AssertionError: expected false to be true // Object.is equality",
		"green": "pnpm --filter front exec vitest run src/server.static.test.ts (Test Files 1 passed; Tests 6 passed.)",
		"mutations": [
			"Remove expect(response.ok).toBe(true): the named status-axis test becomes green.",
			"Remove expect(body).toBe(INDEX_HTML_BODY): the named body-axis test becomes green.",
			"Remove the outer expect(...).toBe(true): the caught AssertionError is no longer reasserted and both named tests become green."
		],
		"adverseSearch": [
			"Replace the status assertion with response.status === 200: the status-axis test remains red.",
			"Replace the exact-body assertion with a non-empty-body check: the body-axis test remains red for the empty-body variant.",
			"Change the response body to another non-empty value while retaining the exact-body assertion: both named tests remain red."
		]
	}
}
