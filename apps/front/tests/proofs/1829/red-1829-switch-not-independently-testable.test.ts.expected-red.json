{
	"$schema": "../expected-red.schema.json",
	"_comment": "Per-test kept-red expectation manifest (required since #1806 r11 — the runner fails loud when a declared proof lacks it). The declared test MUST fail with an AssertionError on correct code.",
	"expectedRed": [
		{
			"testName": "the verdict-to-counter switch is NOT independently testable (issue #1829)",
			"why": "On correct code the verdict-to-counter switch has been extracted into an independently testable pure function (consume-verdict.mts), so the structural assertion that it is NOT independently testable fails with an AssertionError. If this test passes, the switch was folded back into the untestable script body."
		}
	],
	"measuredAgainst": "1d425ffe7d86fea934ec4053e2d653a77d4f3c93"
}
