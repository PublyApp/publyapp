current_branch=$(git rev-parse --abbrev-ref HEAD)

if [ "$current_branch" = "main" ] || [ "$current_branch" = "develop" ]; then
	# Write to stderr
  echo "Direct push to $current_branch branch is not allowed, please create a new branch then do a pull request" >&2
  exit 1
fi

