{
	// extend your base config to share compilerOptions, etc
	"extends": "tsconfig/tsconfig.base.json",
	"compilerOptions": {
		// ensure that nobody can accidentally use this config for a build
		"noEmit": true,
		// making tsconfig paths work when type checking with tsc
		"baseUrl": ".",
		"paths": {
			"@/front/*": ["./apps/front/src/*"],
			"@/shared/*": ["./packages/shared/*"],
			"@/js-client/*": ["./packages/js-client/*"],
			// for remix aka react-router app
			"~/front/*": ["./apps/front/src/*"],
			// Explicitly map React Router's virtual server-build module for tsc CLI.
			"virtual:react-router/server-build": [
				"./apps/front/.react-router/types/+server-build.d.ts"
			]
		}
	}
}
