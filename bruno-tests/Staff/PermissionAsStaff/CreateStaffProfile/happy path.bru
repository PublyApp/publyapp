meta {
  name: happy path
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/staff/profiles
  body: json
  auth: inherit
}

headers {
  "{{SESSION_HEADER_KEY}}": {{SESSION_TOKEN}}
}

body:json {
  {
    "name": "TEST Bruno",
    "permissions": ["staff.profiles.create_for_staff"]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
